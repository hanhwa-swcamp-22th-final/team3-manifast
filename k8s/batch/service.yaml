apiVersion: v1
kind: Service
metadata:
  name: batch
  namespace: seto
  labels:
    app: batch
spec:
  type: ClusterIP
  selector:
    app: batch
  ports:
    - name: http
      port: 8084
      targetPort: 8084
      protocol: TCP
