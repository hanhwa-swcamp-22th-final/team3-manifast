apiVersion: v1
kind: Service
metadata:
  name: kms
  namespace: seto
  labels:
    app: kms
spec:
  type: ClusterIP
  selector:
    app: kms
  ports:
    - name: http
      port: 8083
      targetPort: 8083
      protocol: TCP
