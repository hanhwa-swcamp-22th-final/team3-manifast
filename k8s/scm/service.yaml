apiVersion: v1
kind: Service
metadata:
  name: scm
  namespace: seto
  labels:
    app: scm
spec:
  type: ClusterIP
  selector:
    app: scm
  ports:
    - name: http
      port: 8082
      targetPort: 8082
      protocol: TCP
